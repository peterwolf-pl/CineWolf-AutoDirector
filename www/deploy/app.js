var fs = require("fs"); var http = require("http"); var path = require("path"); var next = require("next"); var distDir = "build"; if (!fs.existsSync(path.join(__dirname, distDir, "BUILD_ID"))) { console.error("Missing build/BUILD_ID"); process.exit(1); } var passenger = typeof PhusionPassenger !== "undefined"; if (passenger) { PhusionPassenger.configure({ autoInstall: false }); } var port = parseInt(process.env.PORT || "3000", 10); var host = process.env.HOSTNAME || "0.0.0.0"; var app = next({ dev: false, dir: __dirname, hostname: host, port: port, conf: { distDir: distDir } }); var handle = app.getRequestHandler(); app.prepare().then(function () { var server = http.createServer(function (req, res) { handle(req, res); }); if (passenger) { server.listen("passenger", function () { console.log("CineWolf ready (Passenger)"); }); } else { server.listen(port, host, function () { console.log("CineWolf ready " + host + ":" + port); }); } }).catch(function (e) { console.error(e); process.exit(1); });
